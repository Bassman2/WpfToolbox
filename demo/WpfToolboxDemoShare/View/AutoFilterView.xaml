<UserControl x:Class="WpfToolboxDemo.View.AutoFilterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfToolboxDemo.View"
             xmlns:vm="clr-namespace:WpfToolboxDemo.ViewModel"
             d:DataContext="{d:DesignInstance Type=vm:AutoFilterViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:AutoFilterViewModel/>
    </UserControl.DataContext>
    <Grid>
        <DataGrid
            ItemsSource="{Binding Items}"
            CanUserSortColumns="True"
            AutoGenerateColumns="False"
            HeadersVisibility="Column"
            GridLinesVisibility="None" 
            IsSynchronizedWithCurrentItem="True"
            Margin="8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}"/>
                <DataGridAutoFilterEnumColumn Header="Enum A" Binding="{Binding Enum1}" />
                <DataGridAutoFilterEnumColumn Header="Enum B" Binding="{Binding Enum2, Converter={StaticResource descriptionConverter}}"/>
                <DataGridAutoFilterTextColumn Header="Group" Binding="{Binding Group}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
