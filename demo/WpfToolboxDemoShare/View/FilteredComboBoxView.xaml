<UserControl
    x:Class="WpfToolboxDemo.View.FilteredComboBoxView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WpfToolboxDemo.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:WpfToolboxDemo.ViewModel"
    d:DataContext="{d:DesignInstance Type=vm:FilteredComboBoxViewModel}"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="1000">
    <UserControl.DataContext>
        <vm:FilteredComboBoxViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox Margin="3"/>
        
        <!--<Label Grid.Row="1" Grid.Column="0" Content="String" />
        <FilteredComboBox Grid.Row="2" Grid.Column="0" ItemsSource="{Binding StringItems}" SelectedItem="{Binding SelectedStringItem}" Margin="3"/>
        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedStringItem}" Margin="3"/>-->

        <Label Grid.Row="1" Grid.Column="2" Content="String" />
        <!--<FilteredComboBox Grid.Row="2"  Grid.Column="2" ItemsSource="{Binding ClassItems}" SelectedItem="{Binding SelectedClassItem}" DisplayMemberPath="Name" Margin="3"/>-->
        <ComboBox Grid.Row="2"  Grid.Column="2" ItemsSource="{Binding ClassItems}" SelectedItem="{Binding SelectedClassItem}" DisplayMemberPath="Name" 
                  TextSearch.TextPath="Name" 
                  IsTextSearchEnabled="True" 
                  IsTextSearchCaseSensitive="False"  
                  IsEditable="True" Margin="3"/>
        <TextBlock Grid.Row="2" Grid.Column="3" Text="{Binding SelectedClassItem.Name}" Margin="3"/>
        <TextBlock Grid.Row="3" Grid.Column="3" Text="{Binding SelectedClassItem.Value}" Margin="3"/>
    </Grid>
</UserControl>
