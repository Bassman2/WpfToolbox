<UserControl
    x:Class="WpfToolboxDemo.View.ValidationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WpfToolboxDemo.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:conv="clr-namespace:WpfToolbox.Converter;assembly=WpfToolbox"
    xmlns:vm="clr-namespace:WpfToolboxDemo.ViewModel"
    d:DataContext="{d:DesignInstance Type=vm:ValidationViewModel}"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <UserControl.DataContext>
        <vm:ValidationViewModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/WpfToolbox;component/Themes/Validation.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <conv:BoolNotConverter x:Key="BoolNotConverter"/>
            <!--
        <Style x:Key="ToolTipError" TargetType="{x:Type TextBox}" >
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="true">
                    <Setter Property="ToolTip" Value="{Binding RelativeSource={x:Static RelativeSource.Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        -->
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Grid.Column="0" Grid.Row="0" Content="Text"/>
        <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding Text, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" Style="{StaticResource ToolTipError}" Margin="3"/>

        <Button Grid.Column="1" Grid.Row="3" IsEnabled="{Binding HasErrors, Converter={StaticResource BoolNotConverter}}" Content="OK" HorizontalAlignment="Center" Width="100" Margin="3"/>
        <Button Grid.Column="1" Grid.Row="4" IsEnabled="{Binding HasNoErrors}" Content="OK" HorizontalAlignment="Center" Width="100" Margin="3"/>
    </Grid>
</UserControl>
